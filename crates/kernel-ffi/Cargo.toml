[package]
name = "kernel-ffi"
version = "0.1.0"
edition = "2021"

[dependencies]
gpu-hal = { path = "../gpu-hal" }
half = "2"
serde_json = "1"
thiserror = "2"

[build-dependencies]
cc = "1"

[dev-dependencies]
model-store = { path = "../model-store" }
